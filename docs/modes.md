# Tool modes

> Generated by `scripts/gen-docs.mjs` from `src/modes/index.ts` — do not edit by hand.

c64 ships **54 tools** in 10 categories. Every tool is a pure function that runs in your browser (in a Web Worker above 150 000 characters); none of them makes a network request.

Pretty / Raw is the switch in every pane's option bar: *Pretty* is the formatted or structured result, *Raw* the minified or literal one. Tools marked "—" produce a single output.

## Start

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **Auto detect** <br><sub>`auto`</sub> | Paste anything — c64 recognises the format and picks the right tool. | — | — |

## JSON

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **JSON Format / Validate** <br><sub>`json`</sub> | Prettify, minify and validate JSON — with exact error locations and fix hints. | yes | **Indent**: `2` / `4` / `tab` (default `2`)<br>**Sort keys** (off by default) |
| **JSON Tree View** <br><sub>`json-tree`</sub> | Explore nested JSON as a collapsible tree; copy any value or its path. | — | — |
| **JSON Path** <br><sub>`json-path`</sub> | Click a value to get its JSONPath, or run live queries like $..name. | — | **JSONPath query**: free text |
| **JSON Graph** <br><sub>`json-graph`</sub> | See the structure as a tidy tree of cards: zoom, pan, search, collapse branches and inspect any value. | — | — |
| **JSON → Table** <br><sub>`json-table`</sub> | Turn an array of JSON objects (or NDJSON) into a sortable, filterable table and export it as CSV, TSV or Markdown. | yes | **Sort by**: free text<br>**Filter**: free text<br>**Output**: `csv` / `tsv` / `markdown` / `json` (default `csv`)<br>**Flatten nested** (on by default)<br>**Sort columns** (off by default) |
| **JSON Schema Validate** <br><sub>`json-schema`</sub> | Validate a JSON document against a JSON Schema (draft-07 to 2020-12) and list every violation with its path. | yes | — |
| **JSON Flatten / Unflatten** <br><sub>`json-flatten`</sub> | Flatten nested JSON into dotted-path keys (or JSON Pointers) and rebuild the nesting again. | yes | **Direction**: `auto` / `flatten` / `unflatten` (default `auto`)<br>**Style**: `dot` / `bracket` / `slash` (default `dot`)<br>**Delimiter**: free text (default `.`)<br>**Arrays as [0]** (on by default)<br>**Rebuild arrays** (on by default) |
| **JSON Sort & Normalise** <br><sub>`json-sort`</sub> | Deep-sort object keys, sort and dedupe arrays, drop nulls and empties, and round numbers for stable comparisons. | yes | **Key order**: `asc` / `desc` / `natural` / `length` (default `asc`)<br>**Sort arrays** (off by default)<br>**Array key**: free text<br>**Dedupe arrays** (off by default)<br>**Drop nulls** (off by default)<br>**Drop empty** (off by default)<br>**Numbers**: `keep` / `round2` / `integers` (default `keep`)<br>**Indent**: `2` / `4` / `tab` (default `2`) |
| **JSON → Types** <br><sub>`json-to-types`</sub> | Generate TypeScript, Zod, Python TypedDict, Go struct or JSON Schema definitions from a JSON sample. | — | **Target**: `typescript` / `zod` / `python` / `go` / `jsonschema` (default `typescript`)<br>**Root name**: free text (default `Root`)<br>**Missing keys optional** (on by default) |

## Formats

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **XML Format / Validate** <br><sub>`xml`</sub> | Pretty-print or minify XML and check it is well-formed. | yes | **Indent**: `2` / `4` / `tab` (default `2`) |
| **YAML** <br><sub>`yaml`</sub> | Validate and reformat YAML; Raw shows the equivalent JSON. | yes | **Indent**: `2` / `4` (default `2`) |
| **TOML ↔ JSON** <br><sub>`toml`</sub> | Parse TOML 1.0 into JSON with exact error positions, or write JSON out as TOML tables. | yes | **Direction**: `auto` / `toJson` / `toToml` (default `auto`) |
| **CSV / TSV** <br><sub>`csv`</sub> | Parse CSV or TSV into a table; delimiter is detected for you. | yes | **Delimiter**: `auto` / `,` / `	` / `;` / `|` (default `auto`)<br>**Header row** (on by default) |
| **HTML Format / Minify** <br><sub>`html`</sub> | Pretty-print or minify HTML, tolerating real-world markup and flagging unclosed or stray tags. | yes | **Indent**: `2` / `4` / `tab` (default `2`)<br>**Keep comments (Raw)** (off by default)<br>**Strict nesting** (off by default) |
| **SQL Formatter** <br><sub>`sql`</sub> | Lay out SQL one clause per line with consistent keyword casing, or squash it onto one line per statement. | yes | **Indent**: `2` / `4` / `tab` (default `2`)<br>**Uppercase keywords** (on by default)<br>**Commas**: `end` / `start` (default `end`) |
| **Convert** <br><sub>`convert`</sub> | Convert between JSON, YAML, XML, CSV and TOML in any direction. | — | **From**: `auto` / `json` / `yaml` / `xml` / `csv` / `toml` (default `auto`)<br>**To**: `json` / `yaml` / `xml` / `csv` / `toml` (default `yaml`)<br>**Typed CSV cells** (off by default) |

## Compare

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **JSON Compare** <br><sub>`json-diff`</sub> | Compare two JSON documents structurally and produce a change report or JSON Patch. | yes | **Arrays**: `index` / `lcs` / `key` / `set` (default `index`)<br>**Key fields**: free text (default `id`)<br>**Ignore case** (off by default)<br>**Trim strings** (off by default)<br>**Numeric strings** (off by default)<br>**Ignore paths**: free text<br>**Only changes** (on by default) |
| **XML Compare** <br><sub>`xml-diff`</sub> | Compare two XML documents by structure — attributes, text and child elements — ignoring formatting. | yes | **Arrays**: `index` / `lcs` / `key` / `set` (default `index`)<br>**Key fields**: free text (default `id`)<br>**Ignore case** (off by default)<br>**Trim strings** (on by default)<br>**Numeric strings** (off by default)<br>**Ignore paths**: free text<br>**Only changes** (on by default) |
| **YAML Compare** <br><sub>`yaml-diff`</sub> | Compare two YAML documents by their parsed values, ignoring comments and formatting. | yes | **Arrays**: `index` / `lcs` / `key` / `set` (default `index`)<br>**Key fields**: free text (default `id`)<br>**Ignore case** (off by default)<br>**Trim strings** (off by default)<br>**Numeric strings** (off by default)<br>**Ignore paths**: free text<br>**Only changes** (on by default) |
| **JSON Patch / Merge Patch** <br><sub>`json-patch`</sub> | Apply an RFC 6902 JSON Patch or RFC 7386 Merge Patch to a document and see the result. | yes | **Format**: `auto` / `json-patch` / `merge-patch` (default `auto`)<br>**Show diff** (off by default) |
| **Text Diff** <br><sub>`diff`</sub> | Compare two texts by line, word or character with a side-by-side or inline view. | — | **Granularity**: `line` / `word` / `char` (default `line`)<br>**Ignore whitespace** (off by default)<br>**Ignore case** (off by default) |
| **List Compare** <br><sub>`list-compare`</sub> | Compare two line lists: items in both, only in A, only in B, union and symmetric difference. | — | **Show**: `all` / `intersection` / `onlyA` / `onlyB` / `union` / `symmetric` (default `all`)<br>**Ignore case** (off by default)<br>**Trim** (on by default)<br>**Unique** (on by default) |

## Encoding

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **Base64** <br><sub>`base64`</sub> | Encode text to Base64 or decode it back — standard or URL-safe. | yes | **Direction**: `auto` / `encode` / `decode` (default `auto`)<br>**URL-safe** (off by default) |
| **JWT (decode only)** <br><sub>`jwt`</sub> | Split and read a JWT's header and claims. Decode only — never verifies. | — | — |
| **Hex / Binary / Bytes** <br><sub>`hex`</sub> | Convert text to hex or binary bytes and back, or view an xxd-style hexdump. | — | **Mode**: `auto` / `text-to-hex` / `hex-to-text` / `hexdump` / `text-to-binary` / `binary-to-text` (default `auto`)<br>**Separator**: `none` / `space` / `colon` / `0x` (default `space`)<br>**Uppercase** (off by default) |
| **Number Base Converter** <br><sub>`base-n`</sub> | Convert integers of any size between binary, octal, decimal, hex and base-36. | — | **From**: `auto` / `2` / `8` / `10` / `16` / `36` (default `auto`)<br>**To**: `all` / `2` / `8` / `10` / `16` / `36` (default `all`)<br>**Uppercase** (off by default) |
| **Gzip / Deflate** <br><sub>`gzip`</sub> | Compress text to gzip, zlib or raw deflate (as Base64 or hex) and decompress it back, detecting the header automatically. | — | **Direction**: `auto` / `compress` / `decompress` (default `auto`)<br>**Algorithm**: `gzip` / `deflate` / `deflate-raw` (default `gzip`)<br>**Encoding**: `base64` / `hex` (default `base64`) |
| **Data URL / File Base64** <br><sub>`data-url`</sub> | Decode data: URLs to text or a hex dump with image preview and type detection, or wrap text and base64 into a data URL. | — | **Direction**: `auto` / `decode` / `encode` (default `auto`)<br>**MIME**: free text (default `text/plain;charset=utf-8`)<br>**Base64** (on by default) |

## Text

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **Minify / Prettify** <br><sub>`minify`</sub> | Squeeze or expand JSON, XML or CSS without changing anything else. | yes | **Language**: `auto` / `json` / `xml` / `css` (default `auto`)<br>**Indent**: `2` / `4` / `tab` (default `2`) |
| **CSS** <br><sub>`css`</sub> | Beautify or minify CSS with structural validation. | yes | **Indent**: `2` / `4` / `tab` (default `2`) |
| **Markdown Preview** <br><sub>`markdown`</sub> | Render Markdown (CommonMark plus GFM tables and task lists) to a live preview and clean HTML. | yes | **Show HTML** (off by default) |
| **HTML → Markdown** <br><sub>`html-to-markdown`</sub> | Convert HTML to GitHub Flavored Markdown — headings, lists, tables, code blocks, links and images. | — | **Bullet**: `-` / `*` / `+` (default `-`)<br>**Emphasis**: `*` / `_` (default `*`)<br>**ATX headings** (on by default)<br>**Reference links** (off by default)<br>**Keep unknown HTML** (off by default) |
| **Case Converter** <br><sub>`case`</sub> | Convert identifiers and text between camelCase, snake_case, kebab-case, Title Case and more. | — | **Convert to**: `camel` / `pascal` / `snake` / `screaming` / `kebab` / `title` / `sentence` / `lower` / `upper` / `dot` / `path` / `header` / `alternating` (default `camel`)<br>**Per line** (on by default) |
| **Line Tools** <br><sub>`lines`</sub> | Sort, dedupe, number, wrap, shuffle, join or split the lines of any text. | — | **Operation**: `sort` / `sort-desc` / `sort-natural` / `sort-length` / `reverse` / `shuffle` / `dedupe` / `dedupe-consecutive` / `trim` / `remove-empty` / `number` / `unnumber` / `prefix-suffix` / `join` / `split` / `unique-count` / `wrap` / `truncate` (default `sort`)<br>**Ignore case** (off by default)<br>**Trim first** (off by default)<br>**Separator**: free text (default `, `)<br>**Prefix**: free text<br>**Suffix**: free text<br>**Width**: free text (default `80`)<br>**Shuffle seed**: free text (default `1`) |
| **Escape / Unescape** <br><sub>`escape`</sub> | Quote text for JSON, JavaScript, CSV, shell, regex, XML or SQL — and unescape it again. | — | **Language**: `json` / `javascript` / `csv` / `shell` / `regex` / `xml` / `sql` (default `json`)<br>**Direction**: `escape` / `unescape` (default `escape`)<br>**Surrounding quotes** (off by default) |
| **String Utilities** <br><sub>`string-utils`</sub> | Slugify, strip accents, reverse, ROT13/47, NATO spelling, mask, Roman numerals, Unicode escapes and code points. | — | **Operation**: `slugify` / `deburr` / `reverse` / `rot13` / `rot47` / `nato` / `obfuscate` / `roman` / `unicode-escape` / `codepoints` (default `slugify`)<br>**Per line** (on by default)<br>**Lowercase** (on by default)<br>**Separator**: free text (default `-`)<br>**Max length**: free text<br>**Keep**: free text (default `2`) |
| **Text Statistics** <br><sub>`text-stats`</sub> | Count characters, words, sentences and paragraphs, estimate reading time and list the most frequent words. | — | **Ignore stop words** (on by default) |

## Web

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **URL Encode / Decode** <br><sub>`url`</sub> | Percent-encode or decode text and break a full URL into its parts. | — | **Direction**: `auto` / `encode` / `decode` (default `auto`)<br>**Mode**: `component` / `uri` / `form` (default `component`) |
| **Query String ↔ JSON** <br><sub>`query-string`</sub> | Convert a URL query string to a JSON object and back, with nested bracket keys. | yes | **Direction**: `auto` / `toJson` / `toQuery` (default `auto`)<br>**Typed values** (off by default) |
| **HTML Entities** <br><sub>`html-entities`</sub> | Encode special characters as HTML entities or decode named and numeric references back to text. | — | **Direction**: `auto` / `encode` / `decode` (default `auto`)<br>**Encode scope**: `minimal` / `named` / `all-non-ascii` (default `minimal`) |
| **Color Converter** <br><sub>`color`</sub> | Convert colours between hex, rgb, hsl and hwb with contrast checks, nearest named colour and a tint/shade scale. | — | **Output**: `all` / `hex` / `rgb` / `hsl` (default `all`) |

## Crypto & IDs

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **Hash / Checksum** <br><sub>`hash`</sub> | MD5, SHA-1/256/384/512 and CRC32 of the input, optionally as an HMAC. | — | **Algorithm**: `all` / `md5` / `sha1` / `sha256` / `sha384` / `sha512` / `crc32` (default `all`)<br>**Encoding**: `hex` / `base64` (default `hex`)<br>**Uppercase** (off by default)<br>**Trim** (off by default)<br>**HMAC** (off by default)<br>**Key**: free text |
| **UUID / ULID / Random** <br><sub>`uuid`</sub> | Generate UUID v4/v7, ULID, nanoid, random bytes and passwords — or decode ids you paste. | — | **Kind**: `uuid-v4` / `uuid-v7` / `ulid` / `nanoid` / `random-hex` / `random-base64` / `password` (default `uuid-v4`)<br>**Count**: `1` / `5` / `10` / `25` / `100` (default `5`)<br>**Length**: `8` / `16` / `24` / `32` / `64` (default `16`)<br>**Symbols** (on by default)<br>**Uppercase** (off by default)<br>**Hyphens** (on by default) |
| **TOTP / HOTP** <br><sub>`totp`</sub> | Generate one-time codes from a Base32 secret or otpauth:// URL, entirely in the browser. | yes | **Mode**: `totp` / `hotp` (default `totp`)<br>**Algorithm**: `SHA1` / `SHA256` / `SHA512` (default `SHA1`)<br>**Digits**: `6` / `8` (default `6`)<br>**Period**: `30` / `60` (default `30`)<br>**Counter**: free text (default `0`) |

## Generators

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **QR Code** <br><sub>`qr-code`</sub> | Encode text, URLs, Wi-Fi credentials, vCards and more as a QR code, rendered locally as SVG. | yes | **Preset**: `text` / `url` / `wifi` / `vcard` / `email` / `phone` / `sms` (default `text`)<br>**ECC**: `L` / `M` / `Q` / `H` (default `M`)<br>**Size**: `128` / `256` / `512` (default `256`)<br>**Dark**: free text (default `#000000`)<br>**Light**: free text (default `#ffffff`) |
| **Lorem Ipsum / Fake Data** <br><sub>`lorem`</sub> | Generate placeholder text, titles, names, emails, addresses or JSON records, optionally from a seed. | yes | **Kind**: `words` / `sentences` / `paragraphs` / `title` / `names` / `emails` / `addresses` / `json-records` (default `paragraphs`)<br>**Count**: `1` / `3` / `5` / `10` / `25` / `50` (default `3`)<br>**Start with "Lorem ipsum"** (on by default)<br>**Seed**: free text |

## Developer

| Tool | What it does | Pretty / Raw | Options |
|---|---|---|---|
| **Regex Tester** <br><sub>`regex`</sub> | Try a JavaScript regular expression against text: highlight matches, list groups, replace or split. | — | **Pattern**: free text<br>**Flags**: free text (default `g`)<br>**Action**: `match` / `replace` / `split` / `test` (default `match`)<br>**Replacement**: free text |
| **Unix Time ↔ Date** <br><sub>`timestamp`</sub> | Convert unix timestamps (s, ms, µs, ns) and dates to every common form, with relative time. | — | **Output**: `all` / `seconds` / `milliseconds` / `iso` (default `all`)<br>**Timezone**: free text (default `UTC`) |
| **Cron Expression** <br><sub>`cron`</sub> | Explain a cron schedule in plain English and list its next run times in any time zone. | — | **Time zone**: free text (default `UTC`) |
| **Math Evaluator** <br><sub>`math-eval`</sub> | Evaluate arithmetic expressions line by line with functions, variables, percentages and exact big integers. | yes | **Format**: `auto` / `fixed2` / `scientific` / `hex` / `binary` (default `auto`) |
| **Unit Converter** <br><sub>`unit-convert`</sub> | Convert quantities between units of 13 categories — length, mass, temperature, data, speed and more. | — | **To**: free text<br>**Precision**: `3` / `6` / `10` (default `6`) |
| **IP / Subnet Calculator** <br><sub>`ip-subnet`</sub> | Network, broadcast, mask, host range, address type and reverse DNS for IPv4 and IPv6 addresses, CIDR blocks and ranges. | yes | **Split**: `none` / `+1` / `+2` / `+3` / `+4` (default `none`) |
| **chmod Calculator** <br><sub>`chmod`</sub> | Convert Unix permissions between octal, symbolic and a read/write/execute grid, including setuid, setgid and sticky bits. | — | **File**: free text (default `file`) |

## Rich views

Some tools return a structured view instead of plain text (the **Copy** and **Download** buttons still use the text form): JSON Tree / Path (collapsible tree, path bar, live queries), JSON Graph (zoomable card tree), CSV (table), JWT (decoded segments and claims), URL (breakdown + query parameters), Case Converter (every variant at once), Hash (digest table), Number Base (per-base table), Unix Time (date cards), UUID (decoded ids), Text Diff (side-by-side / inline), JSON / XML / YAML Compare (aligned trees with filters and navigation), List Compare (set tiles), Schema Validate (error table), Regex (highlighted matches + groups), Markdown (sanitised preview), Text Statistics (tiles), Cron (description + next runs), Color (swatches, scale, contrast), QR Code (rendered SVG), TOTP (live code and countdown), Unit Converter, IP / Subnet and chmod (cards) and Data URL (file preview).
